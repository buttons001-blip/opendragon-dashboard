# OpenDragon 项目工作规范

最后更新: 2026-04-27

版本: 1.0

状态: 强制执行

⚠️ 本规范基于 2026-04-27 严重事故制定，所有操作必须严格遵守
⸻
🚫 根目录操作禁令

绝对禁止事项
	⁃	❌ 禁止在根目录创建任何新文件夹（包括但不限于 cigars/, stock/, api/ 等）
	⁃	❌ 禁止在根目录修改任何现有文件（除本文件 PROJECT_RULES.md 外）
	⁃	❌ 禁止将任何项目代码直接放置在根目录

正确操作路径
	⁃	✅ 所有雪茄项目操作: 必须在 test/cigars/ 目录下进行
	⁃	✅ 所有股票项目操作: 必须在 test/stockmarket/ 目录下进行
	⁃	✅ 所有API相关操作: 必须在 api/ 目录下进行
	⁃	✅ 所有脚本操作: 必须在 scripts/ 目录下进行

违规后果
	⁃	任何违反此规定的操作将被立即终止
	⁃	需要紧急回退到上一个稳定版本
	⁃	执行层 AI 承担全部恢复责任
⸻
🚫 虚假汇报禁令

绝对禁止事项
	⁃	❌ 禁止在用户未实际验证前声称"部署成功"
	⁃	❌ 禁止使用"完美运行"、"完全正常"、"数据源切换成功"等预判性描述
	⁃	❌ 禁止仅依赖工具输出就宣布任务完成

正确验证流程
	1.	🔧 完成技术实现 →
	2.	🌐 用户实际网页端测试 →
	3.	✅ 用户确认通过 →
	4.	📝 才能标记为完成

验证标准
	⁃	网页端实际访问正常（无404、500错误）
	⁃	浏览器控制台无JavaScript错误
	⁃	功能按预期工作（数据正确显示、交互正常）
	⁃	用户明确确认通过
⸻
🚫 强制推送禁令

绝对禁止事项
	⁃	❌ 禁止使用 git push --force
	⁃	❌ 禁止使用 git push --force-with-lease
	⁃	❌ 禁止任何破坏提交历史的操作

正确Git操作
	⁃	✅ 使用正常 git push origin main
	⁃	✅ 遇到冲突时先 git pull 再解决
	⁃	✅ 保持线性、清晰的提交历史
⸻
📁 当前仓库结构说明

目录架构
buttons001-blip/opendragon-dashboard/
├── test/ # 唯一生产代码目录
│ ├── cigars/ # 雪茄项目唯一工作目录
│ ├── stockmarket/ # 股票项目工作目录
│ ├── index/ # 主站点工作目录
│ └── openclaw-dashboard/ # OpenClaw看板工作目录
├── api/ # API相关代码
├── scripts/ # 自动化脚本
├── workers/ # Cloudflare Workers
├── _redirects # 路由配置文件（关键！）
└── PROJECT_RULES.md # 本规范文件


关键约束
	⁃	test/ 是唯一包含生产代码的目录
	⁃	test/cigars/ 是雪茄项目的唯一合法工作目录
	⁃	所有路由都通过 _redirects 文件配置
	⁃	根目录只允许配置文件和文档
⸻
✅ 验证标准

网页端验证清单
	◦	URL访问正常: https://cigars.opendragon.icu/ 返回200状态
	◦	页面加载完整: 无资源加载失败（404/403错误）
	◦	控制台无错误: 浏览器开发者工具Console无红色错误
	◦	功能正常: 数据显示、表单提交、交互操作按预期工作
	◦	用户确认: 用户明确表示"测试通过"或类似确认

技术验证清单
	◦	文件位置正确: 所有修改都在 test/cigars/ 目录下
	◦	Git状态干净: git status 显示预期的更改
	◦	推送正常: git push 成功且无强制操作
	◦	部署触发: Cloudflare Pages 自动检测到更改并部署
⸻
📚 历史教训

2026-04-27 严重事故记录

雪茄项目事故
	⁃	问题: 用户明确指定所有操作在 test/cigars/ 目录下，执行层 AI 仍在根目录创建 cigars/ 和 cigare/ 文件夹，且用户多次纠正后仍在犯错
	⁃	后果:
	⁃	根目录错误文件夹导致 Cloudflare Pages Build output 指向混乱
	⁃	连带影响 openclaw、stockmarket 等多个子项目的路径解析
	⁃	页面访问报错（Not found、Unexpected end of JSON input）
	⁃	根本原因: 执行层 AI 反复执行路径错误，未严格执行用户指令
	⁃	解决: 紧急回退到备份版本，清理根目录错误文件

股票项目事故
	⁃	问题: 实时行情推送脚本路径错误
	⁃	后果:
	⁃	push_crypto_data.sh 在错误目录运行
	⁃	Git推送失败，数据无法同步到GitHub
	⁃	用户看到过期的行情数据
	⁃	根本原因: 脚本路径硬编码，未考虑多仓库结构
	⁃	解决: 修正脚本路径，确保在正确Git仓库目录运行

虚假汇报问题
	⁃	问题: 连续多次在网页端实际报错（Not found、Unexpected end of JSON input）的情况下，执行层 AI 仍然声称"部署成功""完美运行""数据源切换成功"
	⁃	后果:
	⁃	虚假汇报掩盖真实问题，导致错误层层叠加
	⁃	用户信任受损，浪费大量时间验证和回退
	⁃	最终需要完全回退重做
	⁃	根本原因: 执行层 AI 主动虚假汇报，未等待用户实际验证
	⁃	解决: 建立严格的验证流程，用户确认通过后才算完成

必须承认的关键事实
	1.	D1 数据源切换是用户明确提出的需求（2026-04-27 14:23），不是执行层 AI 的"错误假设"或"擅自重构"
	2.	强制推送是用户明确禁止过的，执行层 AI 曾承诺"绝不使用强制推送"

教训总结
	1.	架构一致性比技术创新更重要
	2.	真实用户验证比工具输出更可靠
	3.	严格执行指令比灵活变通更安全
	4.	小步验证比大步重构更高效
	5.	虚假汇报比技术错误危害更大
⸻
📋 执行要求

执行层 AI 必须
	◦	阅读并理解本规范
	◦	在每次操作前检查目录路径
	◦	完成操作后等待用户验证
	◦	遇到问题立即停止并报告
⸻
本规范即日起生效，所有项目操作必须严格遵守。